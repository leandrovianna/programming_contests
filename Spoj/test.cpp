//Spoj - Life, the Universe, and Everything - TEST
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    int x;

    while (cin >> x, x != 42) {
        cout << x << endl;
    }
    return 0;
}
