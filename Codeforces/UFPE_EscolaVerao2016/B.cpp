#include <iostream>
#include <queue>
using namespace std;

int main() {
	vector<string> v;
	int n, p;
	string s;

	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> s;
		v.push_back(s);
	}

	p = n;

	while (v.size()) {
		
	}

	return 0;
}
