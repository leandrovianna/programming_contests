// Codeforces - Haunted Graveyard - 102470F - SWERC 2009
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int w, h, g, e;

  while (cin >> w >> h, w && h) {
    cin >> g;
    for (int i = 0; i < g; i++) {
      cin >> x >> y;
      mat[x][y] = 2;
    }
  }
  return 0;
}
