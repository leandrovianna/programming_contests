// Codeforces - Matrix - 365C
#include <bits/stdc++.h>
using namespace std;

int main() {
    int a;
    string s;
    set<int> x, y;

    cin >> a >> s;

    for (const auto &c : s) {
        for (const auto &c : s) {

        }
    }

    return 0;
}
