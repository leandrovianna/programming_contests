// Codeforces - Remove a Progression - 1194A
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  int t, n, x;
  cin >> t;

  while (t--) {
    cin >> n >> x;
    cout << 2 * x << "\n";
  }
  return 0;
}
