// Codeforces - Mezo Playing Zoma - 1285A
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    int ans = n+1;
    char ch;

    for (int i = 0; i < n; i++) {
        cin >> ch;
    }

    cout << ans << "\n";

    return 0;
}
